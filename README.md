# langchain_documentation_helper

load Read the docs documentation from langchain and split it into chunks, stores it in pinecone db, and uses LLM for chatting  with the documentation using a streamlit UI
